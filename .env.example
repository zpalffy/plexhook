# Port to listen on (default: 8001)
PORT=8001

# Directory where the Plex thumbnail is saved (default: system temp dir)
COVER_PATH=/tmp

# Optional shared secret — if set, Plex must include ?token=<value> in the webhook URL
PLEX_TOKEN=
